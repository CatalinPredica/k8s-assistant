replicaCount: 1

image:
  backend:
    repository: catalinpredica/k8s-assistant-backend
    tag: "0.1.1"
    pullPolicy: IfNotPresent
  frontend:
    repository: catalinpredica/k8s-assistant-frontend
    tag: "0.1.1"
    pullPolicy: IfNotPresent

service:
  backend:
    type: ClusterIP
    port: 8000
  frontend:
    type: ClusterIP
    port: 80

ingress:
  enabled: false
  className: nginx
  hosts:
    - host: assistant.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}

serviceAccount:
  create: true
  name: ""

rbac:
  create: true

config:
  BACKEND_LOG_LEVEL: info

secret:
  enabled: true
  apiKey: "changeme"   # Example, replace in production